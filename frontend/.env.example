VITE_API_BASE=https://your-api-id.execute-api.us-west-2.amazonaws.com
# Optional: override map style. Leave blank to use bundled src/mapstyles/osm-bright-osmusa.json
VITE_MAP_STYLE=
# Optional: serve POI tiles directly from PMTiles (e.g. CloudFront URL)
VITE_PMTILES_URL=https://your-cloudfront-domain/pois.pmtiles
# When VITE_PMTILES_URL is set, API calls are skipped and VITE_API_BASE can be omitted.
